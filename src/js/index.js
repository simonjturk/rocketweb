// JS Goes here - ES6 supported
var AOS = require('aos');
AOS.init({
    duration: 1200,
});

window.addEventListener('load', AOS.refresh);

import "./validate-contact-form.js"

import "../css/main.css";
